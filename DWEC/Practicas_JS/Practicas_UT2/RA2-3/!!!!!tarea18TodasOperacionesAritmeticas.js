let arrValores = [1,2,3,4,5,6,7,8,9];
let simbolo = "+";
let resultado = 0


let aritmetico = function(operacion,...valores){

    operacion = simbolo;
    valores = arrValores;

    for(let i=0; i<=valores.length; 1++){
        resultado = `${valores[0]} ${operacion} ${[]}`;
    }

}

